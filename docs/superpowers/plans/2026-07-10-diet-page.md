# 饮食记录页面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增饮食记录页面，支持自然语言文本录入、营养解析、AI 分析建议和自由对话

**Architecture:** 前端 `diet.html` 独立页面 + 内置食物库即时匹配，未匹配项走后端 AI 代理兜底。后端新增 `diet_records` 表和 user profile 字段，AI 代理透传 `localhost:20128`

**Tech Stack:** FastAPI + SQLite + vanilla HTML/CSS/JS + requests (后端 AI 代理)

---

### Task 1: 后端 — 用户 Profile 扩展

**Files:**
- Modify: `server.py` (扩展 users 表，新增 API)

**Interfaces:**
- Consumes: 现有 FastAPI app、get_db()、get_current_user()
- Produces: `GET /api/profile` → `{height, weight, goal}`; `PUT /api/profile` 接收 `{height, weight, goal}`

- [ ] **Step 1: 添加 ProfileRequest 模型和 users 表字段迁移**

在 `server.py` 的 `class LoginRequest` 后添加：

```python
class ProfileRequest(BaseModel):
    height: Optional[float] = None
    weight: Optional[float] = None
    goal: Optional[str] = None
```

在 `init_db()` 的 `CREATE TABLE IF NOT EXISTS users` 块后添加迁移：

```python
def migrate_profile():
    conn = get_db()
    for col in ["height", "weight", "goal"]:
        try:
            conn.execute(f"ALTER TABLE users ADD COLUMN {col} TEXT")
        except:
            pass
    conn.commit()
    conn.close()

migrate_profile()
```

- [ ] **Step 2: 添加 Profile API 端点**

在 `def login` 后添加：

```python
@app.get("/api/profile")
def get_profile(user_id: int = Depends(get_current_user)):
    conn = get_db()
    row = conn.execute("SELECT height, weight, goal FROM users WHERE id = ?", (user_id,)).fetchone()
    conn.close()
    return {"height": row["height"], "weight": row["weight"], "goal": row["goal"]}

@app.put("/api/profile")
def update_profile(req: ProfileRequest, user_id: int = Depends(get_current_user)):
    conn = get_db()
    updates = {}
    if req.height is not None: updates["height"] = req.height
    if req.weight is not None: updates["weight"] = req.weight
    if req.goal is not None: updates["goal"] = req.goal
    if updates:
        sets = ", ".join(f"{k}=?" for k in updates)
        conn.execute(f"UPDATE users SET {sets} WHERE id=?", list(updates.values()) + [user_id])
        conn.commit()
    conn.close()
    return {"status": "ok"}
```

- [ ] **Step 3: 验证**

```bash
curl -X POST http://localhost:9081/api/login -H "Content-Type: application/json" -d '{"username":"...","password":"..."}'
# 拿到 token 后:
curl -X PUT http://localhost:9081/api/profile -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"height":175,"weight":70,"goal":"gain"}'
curl http://localhost:9081/api/profile -H "Authorization: Bearer $TOKEN"
```

- [ ] **Step 4: Commit**

```bash
git add server.py
git commit -m "feat: 用户 profile (身高/体重/目标)"
```

---

### Task 2: 后端 — diet_records 表 & CRUD

**Files:**
- Modify: `server.py` (新增表和 API)

**Interfaces:**
- Produces: `GET /api/diet/{date}` → `{date, meals, raw_text}`; `PUT /api/diet/{date}` 接收 `{meals, raw_text}`

- [ ] **Step 1: 添加 diet_records 表和 DietRequest 模型**

在 `init_db()` 末尾追加：

```python
conn.execute("""
    CREATE TABLE IF NOT EXISTS diet_records (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        user_id INTEGER NOT NULL,
        date TEXT NOT NULL,
        meals TEXT NOT NULL DEFAULT '[]',
        raw_text TEXT DEFAULT '',
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (user_id) REFERENCES users(id),
        UNIQUE(user_id, date)
    )
""")
```

在 `class ProfileRequest` 后添加：

```python
class DietRequest(BaseModel):
    meals: str = "[]"
    raw_text: str = ""
```

- [ ] **Step 2: 添加 Diet API**

在 `update_profile` 后添加：

```python
@app.get("/api/diet/{date}")
def get_diet(date: str, user_id: int = Depends(get_current_user)):
    conn = get_db()
    row = conn.execute(
        "SELECT meals, raw_text FROM diet_records WHERE user_id=? AND date=?",
        (user_id, date)
    ).fetchone()
    conn.close()
    if row:
        return {"date": date, "meals": json.loads(row["meals"]), "raw_text": row["raw_text"]}
    return {"date": date, "meals": [], "raw_text": ""}

@app.put("/api/diet/{date}")
def save_diet(date: str, req: DietRequest, user_id: int = Depends(get_current_user)):
    conn = get_db()
    conn.execute(
        """INSERT INTO diet_records (user_id, date, meals, raw_text, updated_at)
           VALUES (?, ?, ?, ?, ?)
           ON CONFLICT(user_id, date)
           DO UPDATE SET meals=excluded.meals, raw_text=excluded.raw_text, updated_at=excluded.updated_at""",
        (user_id, date, req.meals, req.raw_text, datetime.now().isoformat())
    )
    conn.commit()
    conn.close()
    return {"status": "ok"}
```

- [ ] **Step 3: 验证**

```bash
curl -X PUT http://localhost:9081/api/diet/2026-07-10 -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"meals":"[]","raw_text":"测试"}'
curl http://localhost:9081/api/diet/2026-07-10 -H "Authorization: Bearer $TOKEN"
```

- [ ] **Step 4: Commit**

```bash
git add server.py
git commit -m "feat: diet_records 表 + CRUD API"
```

---

### Task 3: 后端 — AI 代理接口

**Files:**
- Modify: `server.py` (新增 3 个代理端点)

**Interfaces:**
- Produces: `POST /api/diet/parse`, `POST /api/diet/analyze`, `POST /api/diet/chat`
- 每个端点接收 `{messages: []}` 格式，返回 AI 回复

- [ ] **Step 1: 添加 AI 代理端点**

在文件顶部添加 import：

```python
import requests as http_requests
```

在 `save_diet` 后添加：

```python
AI_BASE = "http://localhost:20128/v1/chat/completions"
AI_MODEL = "auto/best-fast"

SYSTEM_PROMPTS = {
    "parse": "你是一个营养分析助手。用户会输入食物名称和份量，请返回JSON数组，每项包含 name(中文名), weight_g(克), calories(千卡), protein_g, fat_g, carbs_g。只返回JSON，不要其他文字。如果无法识别某食物，返回 {\"unknown\": true, \"name\": \"...\"}。",
    "analyze": "你是一个健身与营养顾问。根据用户的饮食数据和身体数据，给出分析和建议，包括：热量是否达标、碳蛋脂比例是否合理、改进建议。语气专业但友好，200字以内。",
    "chat": "你是一个健身与营养顾问，回答用户关于训练、饮食、恢复等全部健身话题的问题。基于科学依据给出建议，语气友好专业。"
}

class AIRequest(BaseModel):
    messages: list

@app.post("/api/diet/{action}")
def diet_ai(action: str, req: AIRequest, user_id: int = Depends(get_current_user)):
    if action not in SYSTEM_PROMPTS:
        raise HTTPException(status_code=404)
    system_prompt = SYSTEM_PROMPTS[action]
    payload = {
        "model": AI_MODEL,
        "messages": [{"role": "system", "content": system_prompt}] + req.messages,
        "temperature": 0.7
    }
    try:
        resp = http_requests.post(AI_BASE, json=payload, timeout=30)
        return resp.json()
    except Exception as e:
        raise HTTPException(status_code=503, detail=f"AI 服务暂不可用: {str(e)}")
```

- [ ] **Step 2: 验证**

```bash
curl -X POST http://localhost:9081/api/diet/chat -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"messages":[{"role":"user","content":"增肌期每天应该摄入多少蛋白质？"}]}'
```

- [ ] **Step 3: Commit**

```bash
git add server.py
git commit -m "feat: AI 代理接口 (parse/analyze/chat)"
```

---

### Task 4: 前端 — 食物库 food-db.js

**Files:**
- Create: `static/food-db.js`

- [ ] **Step 1: 创建食物库文件**

```javascript
const FOOD_DB = [
  // 主食
  { name:"白米饭", cal:116, protein:2.6, fat:0.3, carbs:25.6, category:"主食" },
  { name:"糙米饭", cal:111, protein:2.6, fat:0.9, carbs:23, category:"主食" },
  { name:"全麦面包", cal:246, protein:8.5, fat:3.4, carbs:44, category:"主食", aliases:["全麦"] },
  { name:"白面包", cal:265, protein:8, fat:3.2, carbs:49, category:"主食" },
  { name:"馒头", cal:221, protein:7, fat:1.1, carbs:45, category:"主食" },
  { name:"面条(煮)", cal:110, protein:3.4, fat:0.3, carbs:24, category:"主食", aliases:["面条","面"] },
  { name:"燕麦片", cal:367, protein:13.5, fat:6.7, carbs:61.6, category:"主食", aliases:["燕麦"] },
  { name:"红薯", cal:86, protein:1.6, fat:0.1, carbs:20.1, category:"主食", aliases:["地瓜","番薯"] },
  { name:"土豆", cal:76, protein:2, fat:0.2, carbs:17.2, category:"主食", aliases:["马铃薯","马铃薯"] },
  { name:"玉米", cal:112, protein:4, fat:1.2, carbs:22.8, category:"主食" },
  { name:"小米粥", cal:46, protein:1.4, fat:0.7, carbs:8.4, category:"主食" },
  { name:"荞麦面", cal:329, protein:11.3, fat:2.7, carbs:66, category:"主食" },
  { name:"意大利面(干)", cal:350, protein:12, fat:1.5, carbs:73, category:"主食", aliases:["意面"] },

  // 肉类
  { name:"鸡胸肉", cal:133, protein:31, fat:1.2, carbs:0, category:"肉类", aliases:["鸡胸","鸡大胸"] },
  { name:"鸡腿肉", cal:181, protein:20, fat:11, carbs:0, category:"肉类", aliases:["鸡腿"] },
  { name:"鸡翅", cal:222, protein:18, fat:16, carbs:0, category:"肉类" },
  { name:"鸡蛋", cal:144, protein:13, fat:10, carbs:1.6, category:"肉类", aliases:["蛋","土鸡蛋"] },
  { name:"鸡蛋白", cal:52, protein:11, fat:0.2, carbs:0.7, category:"肉类", aliases:["蛋清","蛋白"] },
  { name:"鸡蛋黄", cal:322, protein:15, fat:27, carbs:3.6, category:"肉类", aliases:["蛋黄"] },
  { name:"瘦牛肉", cal:125, protein:22, fat:3.5, carbs:0, category:"肉类", aliases:["牛肉","牛腱子"] },
  { name:"牛腩", cal:215, protein:17, fat:16, carbs:0, category:"肉类" },
  { name:"猪瘦肉", cal:143, protein:20, fat:6.2, carbs:1.5, category:"肉类", aliases:["猪肉","里脊肉"] },
  { name:"猪排骨", cal:264, protein:18, fat:21, carbs:0, category:"肉类", aliases:["排骨"] },
  { name:"羊肉", cal:203, protein:19, fat:14, carbs:0, category:"肉类" },
  { name:"鸭肉", cal:240, protein:15.5, fat:19.7, carbs:0.2, category:"肉类" },
  { name:"三文鱼", cal:208, protein:20, fat:13, carbs:0, category:"肉类", aliases:["鲑鱼"] },
  { name:"金枪鱼", cal:130, protein:26, fat:2.5, carbs:0, category:"肉类", aliases:["吞拿鱼","鲔鱼"] },
  { name:"虾仁", cal:99, protein:24, fat:0.3, carbs:0.2, category:"肉类", aliases:["虾","虾肉"] },
  { name:"巴沙鱼", cal:80, protein:15, fat:2, carbs:0, category:"肉类" },
  { name:"带鱼", cal:127, protein:18, fat:5.5, carbs:0, category:"肉类" },

  // 蛋奶
  { name:"牛奶", cal:65, protein:3, fat:3.8, carbs:5.1, category:"蛋奶", aliases:["纯牛奶","鲜奶","牛乳"] },
  { name:"酸奶(无糖)", cal:61, protein:3.5, fat:3.3, carbs:4.2, category:"蛋奶", aliases:["希腊酸奶","无糖酸奶"] },
  { name:"酸奶(风味)", cal:85, protein:3, fat:2.5, carbs:12, category:"蛋奶" },
  { name:"脱脂牛奶", cal:34, protein:3.4, fat:0.1, carbs:5, category:"蛋奶" },
  { name:"奶酪", cal:350, protein:25, fat:27, carbs:1.3, category:"蛋奶", aliases:["芝士","起司"] },
  { name:"蛋白粉", cal:400, protein:80, fat:3.2, carbs:6.3, category:"蛋奶", aliases:["乳清蛋白","whey"] },
  { name:"豆浆", cal:31, protein:2.9, fat:1.4, carbs:1.2, category:"蛋奶" },
  { name:"豆腐", cal:81, protein:8.1, fat:3.7, carbs:4.2, category:"蛋奶" },
  { name:"内酯豆腐", cal:56, protein:5, fat:2.7, carbs:3, category:"蛋奶" },

  // 蔬菜
  { name:"西兰花", cal:34, protein:2.8, fat:0.4, carbs:6.6, category:"蔬菜", aliases:["花椰菜","青花菜"] },
  { name:"菠菜", cal:23, protein:2.9, fat:0.4, carbs:3.6, category:"蔬菜" },
  { name:"生菜", cal:13, protein:0.9, fat:0.2, carbs:2.5, category:"蔬菜" },
  { name:"白菜", cal:13, protein:1.5, fat:0.2, carbs:2.2, category:"蔬菜", aliases:["大白菜","娃娃菜"] },
  { name:"青菜", cal:15, protein:1.5, fat:0.2, carbs:2.8, category:"蔬菜", aliases:["上海青","小油菜","油菜"] },
  { name:"黄瓜", cal:15, protein:0.7, fat:0.1, carbs:3.6, category:"蔬菜" },
  { name:"番茄", cal:18, protein:0.9, fat:0.2, carbs:3.9, category:"蔬菜", aliases:["西红柿","蕃茄"] },
  { name:"胡萝卜", cal:41, protein:1, fat:0.2, carbs:9.6, category:"蔬菜", aliases:["红萝卜"] },
  { name:"芹菜", cal:14, protein:0.7, fat:0.1, carbs:3, category:"蔬菜" },
  { name:"青椒", cal:22, protein:1, fat:0.2, carbs:4.6, category:"蔬菜", aliases:["甜椒","彩椒"] },
  { name:"茄子", cal:25, protein:1, fat:0.2, carbs:5.9, category:"蔬菜" },
  { name:"洋葱", cal:40, protein:1.1, fat:0.1, carbs:9.3, category:"蔬菜" },
  { name:"蒜", cal:126, protein:4.5, fat:0.2, carbs:27, category:"蔬菜", aliases:["大蒜"] },
  { name:"姜", cal:41, protein:1.3, fat:0.7, carbs:7.8, category:"蔬菜" },
  { name:"蘑菇", cal:20, protein:2.7, fat:0.3, carbs:2, category:"蔬菜", aliases:["口蘑","香菇","菌菇"] },
  { name:"木耳", cal:21, protein:1.5, fat:0.2, carbs:3.7, category:"蔬菜" },
  { name:"海带", cal:12, protein:0.7, fat:0.1, carbs:2.5, category:"蔬菜" },
  { name:"玉米笋", cal:24, protein:1.9, fat:0.2, carbs:5, category:"蔬菜" },
  { name:"豆芽", cal:16, protein:1.8, fat:0.1, carbs:2.6, category:"蔬菜" },
  { name:"四季豆", cal:31, protein:2, fat:0.4, carbs:5.6, category:"蔬菜" },
  { name:"芦笋", cal:20, protein:2.2, fat:0.1, carbs:3.9, category:"蔬菜" },
  { name:"秋葵", cal:33, protein:2, fat:0.1, carbs:7, category:"蔬菜" },
  { name:"紫甘蓝", cal:31, protein:1.4, fat:0.2, carbs:7.4, category:"蔬菜" },

  // 水果
  { name:"香蕉", cal:89, protein:1.1, fat:0.3, carbs:22.8, category:"水果" },
  { name:"苹果", cal:52, protein:0.3, fat:0.2, carbs:14, category:"水果" },
  { name:"橙子", cal:47, protein:0.9, fat:0.1, carbs:11.8, category:"水果", aliases:["橙","柳橙"] },
  { name:"蓝莓", cal:57, protein:0.7, fat:0.3, carbs:14.5, category:"水果" },
  { name:"草莓", cal:32, protein:0.7, fat:0.3, carbs:7.7, category:"水果" },
  { name:"葡萄", cal:69, protein:0.7, fat:0.2, carbs:18, category:"水果" },
  { name:"猕猴桃", cal:61, protein:1.1, fat:0.5, carbs:14.7, category:"水果", aliases:["奇异果","kiwi"] },
  { name:"西瓜", cal:30, protein:0.6, fat:0.2, carbs:7.6, category:"水果" },
  { name:"柚子", cal:42, protein:0.8, fat:0.2, carbs:10.7, category:"水果" },
  { name:"火龙果", cal:55, protein:1.1, fat:0.4, carbs:13, category:"水果" },
  { name:"牛油果", cal:160, protein:2, fat:14.7, carbs:8.5, category:"水果", aliases:["鳄梨","avocado"] },

  // 豆类坚果
  { name:"黄豆", cal:446, protein:36.5, fat:19.9, carbs:30.2, category:"豆类坚果", aliases:["大豆"] },
  { name:"花生", cal:567, protein:25.8, fat:49.2, carbs:16.1, category:"豆类坚果" },
  { name:"核桃", cal:654, protein:15.2, fat:65.2, carbs:13.7, category:"豆类坚果" },
  { name:"杏仁", cal:579, protein:21.2, fat:49.9, carbs:21.6, category:"豆类坚果" },
  { name:"腰果", cal:553, protein:18.2, fat:43.8, carbs:30.2, category:"豆类坚果" },
  { name:"芝麻", cal:573, protein:17.7, fat:49.7, carbs:23.4, category:"豆类坚果" },

  // 调味
  { name:"橄榄油", cal:884, protein:0, fat:100, carbs:0, category:"调味" },
  { name:"花生油", cal:884, protein:0, fat:100, carbs:0, category:"调味" },
  { name:"盐", cal:0, protein:0, fat:0, carbs:0, category:"调味" },
  { name:"酱油", cal:60, protein:5.6, fat:0.1, carbs:7.8, category:"调味", aliases:["生抽","老抽"] },
  { name:"醋", cal:30, protein:0.4, fat:0, carbs:4.7, category:"调味" },
  { name:"蜂蜜", cal:304, protein:0.3, fat:0, carbs:82.4, category:"调味" },
  { name:"白糖", cal:387, protein:0, fat:0, carbs:100, category:"调味" },
  { name:"辣椒酱", cal:87, protein:1.5, fat:3.5, carbs:13, category:"调味" },

  // 饮品
  { name:"水", cal:0, protein:0, fat:0, carbs:0, category:"饮品" },
  { name:"黑咖啡", cal:2, protein:0.1, fat:0, carbs:0, category:"饮品", aliases:["美式","黑咖"] },
  { name:"拿铁", cal:56, protein:3, fat:3.5, carbs:4, category:"饮品" },
  { name:"绿茶", cal:1, protein:0, fat:0, carbs:0, category:"饮品" },
  { name:"可乐", cal:42, protein:0, fat:0, carbs:10.6, category:"饮品" },
  { name:"运动饮料", cal:26, protein:0, fat:0, carbs:6.4, category:"饮品", aliases:["佳得乐","宝矿力"] },

  // 外食/速食（常见）
  { name:"皮蛋瘦肉粥", cal:64, protein:4.5, fat:1.2, carbs:9.5, category:"外食" },
  { name:"煎饼", cal:305, protein:8, fat:10, carbs:45, category:"外食" },
  { name:"饺子(猪肉)", cal:240, protein:10, fat:10, carbs:28, category:"外食", aliases:["水饺"] },
  { name:"包子(肉)", cal:226, protein:8.5, fat:8, carbs:30, category:"外食" },
  { name:"馄饨", cal:85, protein:3.5, fat:3, carbs:10, category:"外食", aliases:["抄手","云吞"] },
  { name:"汉堡", cal:520, protein:25, fat:27, carbs:45, category:"外食" },
  { name:"三明治", cal:320, protein:15, fat:12, carbs:38, category:"外食" },
  { name:"披萨(1片)", cal:285, protein:12, fat:10, carbs:36, category:"外食" },
  { name:"麻辣烫", cal:80, protein:5, fat:3, carbs:8, category:"外食" },
  { name:"米线", cal:92, protein:2.5, fat:0.5, carbs:19, category:"外食" },
  { name:"螺蛳粉", cal:320, protein:8, fat:12, carbs:45, category:"外食" },
  { name:"寿司", cal:200, protein:8, fat:2, carbs:38, category:"外食" },
];

const UNIT_CONVERSION = {
  "g": 1, "克": 1, "毫升": 1, "ml": 1,
  "个": {"鸡蛋": 50, "蛋": 50, "包子": 80, "苹果": 200, "香蕉": 120, "橙子": 200, "猕猴桃": 100, "橘子": 150},
  "片": {"面包": 35, "全麦面包": 35, "火腿": 20, "芝士": 20, "奶酪": 20},
  "碗": {"米饭": 200, "白米饭": 200, "粥": 250, "面条": 250, "米线": 300},
  "根": {"香蕉": 120, "黄瓜": 150, "玉米": 200},
  "勺": {"蛋白粉": 30, "奶粉": 25, "蜂蜜": 15},
  "杯": {"牛奶": 250, "酸奶": 200, "豆浆": 250, "水": 250},
  "盒": {"牛奶": 250},
  "颗": {"鸡蛋": 50, "蛋": 50, "核桃": 15},
};
```

- [ ] **Step 2: Commit**

```bash
mkdir -p static
git add static/food-db.js
git commit -m "feat: 内置食物库 food-db.js"
```

---

### Task 5: 前端 — 文本解析器 diet-parser.js

**Files:**
- Create: `static/diet-parser.js`

- [ ] **Step 1: 创建解析器**

```javascript
function parseDietText(text) {
  const meals = [];
  const lines = text.split('\n').filter(l => l.trim());

  let currentMeal = null;

  for (const line of lines) {
    // 匹配 "晚餐:" 或 "晚餐：" 等餐段名
    const mealMatch = line.match(/^(.+?)[：:](.+)/);
    if (mealMatch) {
      if (currentMeal) meals.push(currentMeal);
      currentMeal = { meal: mealMatch[1].trim(), foods: [] };
      parseFoods(mealMatch[2], currentMeal);
    } else {
      if (!currentMeal) currentMeal = { meal: "加餐", foods: [] };
      parseFoods(line, currentMeal);
    }
  }
  if (currentMeal) meals.push(currentMeal);

  return meals;
}

function parseFoods(text, meal) {
  // 按空格、中文逗号分割食物项
  const items = text.split(/[\s,，、]+/).filter(s => s.trim());
  for (const item of items) {
    const parsed = parseFoodItem(item);
    if (parsed) meal.foods.push(parsed);
  }
}

function parseFoodItem(text) {
  // 匹配: 食物名 + 数量 + 单位
  const match = text.match(/^(.+?)\s*(\d+(?:\.\d+)?)\s*(克|g|G|ml|ML|毫升|个|片|碗|根|勺|杯|盒|颗|罐|只|条|块)?$/);
  if (!match) {
    // 纯食物名（无数量），尝试匹配食物库
    const food = matchFood(text);
    if (food) return { name: food.name, weight: 100, unit: "g", ...food };
    return null;
  }

  const name = match[1].trim();
  const weight = parseFloat(match[2]);
  const rawUnit = match[3] || "g";

  // 换算到克
  let grams = weight;
  if (rawUnit !== "g" && rawUnit !== "克" && rawUnit !== "ml" && rawUnit !== "毫升") {
    const conv = UNIT_CONVERSION[rawUnit];
    if (typeof conv === "object") {
      // 按食物名查找换算
      const found = Object.entries(conv).find(([k]) => name.includes(k) || k.includes(name));
      grams = found ? weight * found[1] : weight * 100; // fallback 100g
    } else if (typeof conv === "number") {
      grams = weight * conv;
    }
  }

  const food = matchFood(name);
  if (food) {
    const ratio = grams / 100;
    return {
      name: food.name,
      weight: grams,
      unit: "g",
      calories: Math.round(food.cal * ratio),
      protein: Math.round(food.protein * ratio * 10) / 10,
      fat: Math.round(food.fat * ratio * 10) / 10,
      carbs: Math.round(food.carbs * ratio * 10) / 10,
    };
  }

  // 未匹配
  return { name, weight: grams, unit: "g", unknown: true };
}

function matchFood(name) {
  // 1. 精确匹配
  let found = FOOD_DB.find(f => f.name === name);
  if (found) return found;

  // 2. 别名匹配
  found = FOOD_DB.find(f => f.aliases && f.aliases.includes(name));
  if (found) return found;

  // 3. 子串匹配（长→短）
  const sorted = [...FOOD_DB].sort((a, b) => b.name.length - a.name.length);
  found = sorted.find(f => name.includes(f.name) || f.name.includes(name));
  if (found) return found;

  // 4. 别名子串
  found = sorted.find(f => f.aliases && f.aliases.some(a => name.includes(a) || a.includes(name)));
  return found || null;
}

function summarizeMeals(meals) {
  const total = { calories: 0, protein: 0, fat: 0, carbs: 0 };
  for (const meal of meals) {
    for (const food of meal.foods) {
      if (!food.unknown) {
        total.calories += food.calories || 0;
        total.protein += food.protein || 0;
        total.fat += food.fat || 0;
        total.carbs += food.carbs || 0;
      }
    }
  }
  total.calories = Math.round(total.calories);
  total.protein = Math.round(total.protein * 10) / 10;
  total.fat = Math.round(total.fat * 10) / 10;
  total.carbs = Math.round(total.carbs * 10) / 10;
  return total;
}
```

- [ ] **Step 2: Commit**

```bash
git add static/diet-parser.js
git commit -m "feat: 饮食文本解析器 diet-parser.js"
```

---

### Task 6: 前端 — 饮食记录页面 diet.html

**Files:**
- Create: `diet.html`

这是最大任务。页面结构参考已确认的原型，包含完整交互逻辑。

- [ ] **Step 1: 页面骨架 + 引入 JS**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1.0,user-scalable=no,viewport-fit=cover">
  <meta name="theme-color" content="#1a1a3a">
  <link rel="manifest" href="/manifest.json">
  <title>饮食记录</title>
  <script src="/static/food-db.js"></script>
  <script src="/static/diet-parser.js"></script>
  ...
```

- [ ] **Step 2: CSS**

复用现有玻璃态设计系统（glass-card、渐变色背景、按钮样式、进度条等），与 index.html 保持一致。页面组件样式：餐段卡片、食物行、摘要卡片、AI 面板、聊天输入框。

- [ ] **Step 3: HTML 结构**

按原型布局：
1. Header（标题 + 设置按钮 + AI 分析按钮）
2. 日期选择器
3. 今日总计卡片（热量/蛋白/脂肪/碳水 + 进度条）
4. 餐段卡片列表（餐名 + 食物明细 + 添加食物按钮）
5. 添加餐段按钮
6. 文本录入区（textarea + 解析按钮）
7. AI 分析面板（分析结果 + 聊天输入框）
8. 用户设置弹窗（身高/体重/目标）

- [ ] **Step 4: JS 交互逻辑**

关键函数：

```javascript
// 页面状态
const state = {
  date: today(),
  meals: [],
  rawText: "",
  profile: { height: null, weight: null, goal: null },
  aiMessages: [],
  unknownFoods: [],
};

// 核心操作
function loadDiet(date)         // GET /api/diet/{date} → render
function saveDiet()             // PUT /api/diet/{date}
function parseAndAdd()          // 解析 textarea 文本 → state → render
function analyzeDiet()          // POST /api/diet/analyze
function sendChat()             // POST /api/diet/chat
function resolveUnknowns()      // POST /api/diet/parse → 回填
function loadProfile()          // GET /api/profile
function saveProfile()          // PUT /api/profile
function render()               // 渲染所有
```

初始化：

```javascript
async function init() {
  await loadProfile();
  await loadDiet(state.date);
  render();
}
```

- [ ] **Step 5: 用户设置弹窗**

点击设置图标弹出模态框，包含身高(cm)、体重(kg)、目标(增肌/减脂/维持)三个输入，保存后调 PUT /api/profile 并刷新 AI 面板。

- [ ] **Step 6: Commit**

```bash
git add diet.html
git commit -m "feat: 饮食记录页面 diet.html"
```

---

### 全局：代码风格

- 与现有代码保持一致：ES6+，无框架，内联 CSS 和 JS
- 玻璃态设计：`background: rgba(255,255,255,0.08)` + `backdrop-filter: blur(60px) saturate(200%)`
- 移动端优先，max-width: 430px
- 所有 API 请求附带 `Authorization: Bearer <token>`（token 从 localStorage 读取，与 index.html 一致）
- AI 请求失败时有用户可见的提示，不静默失败
